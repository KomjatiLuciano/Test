package dom.dias;

//import javax.inject.Named;
//
import org.apache.isis.applib.AbstractFactoryAndRepository;
//import org.apache.isis.applib.DomainObjectContainer;
//import org.apache.isis.applib.annotation.DomainService;
//import org.apache.isis.applib.annotation.DomainServiceLayout;
//import org.apache.isis.applib.annotation.MemberOrder;
//import org.apache.isis.applib.annotation.Optionality;
//import org.apache.isis.applib.annotation.Parameter;
//import org.apache.isis.applib.annotation.ParameterLayout;
//
//import dom.doctor.Doctor;

//@DomainService(repositoryFor = Doctor.class)
//@DomainServiceLayout(named = "Doctor", menuBar = DomainServiceLayout.MenuBar.PRIMARY, menuOrder = "3")
//@Named("Dias Doctores")
public class DiasDoctoresServicio extends AbstractFactoryAndRepository {

	// public String iconName() {
	// return "dias";
	// }

	// @MemberOrder(name = "Doctor", sequence = "2.6")
	// public DiasDoctores crearDias(
	// @ParameterLayout(named = "Doctor") final Doctor doc,
	// @ParameterLayout(named = "Lunes") @Parameter(optionality =
	// Optionality.OPTIONAL) final Boolean lunes,
	// @ParameterLayout(named = "Martes") @Parameter(optionality =
	// Optionality.OPTIONAL) final Boolean martes,
	// @ParameterLayout(named = "Miercoles") @Parameter(optionality =
	// Optionality.OPTIONAL) final Boolean miercoles,
	// @ParameterLayout(named = "Jueves") @Parameter(optionality =
	// Optionality.OPTIONAL) final Boolean jueves,
	// @ParameterLayout(named = "Viernes") @Parameter(optionality =
	// Optionality.OPTIONAL) final Boolean viernes,
	// @ParameterLayout(named = "Sabado") @Parameter(optionality =
	// Optionality.OPTIONAL) final Boolean sabado,
	// @ParameterLayout(named = "Domingo") @Parameter(optionality =
	// Optionality.OPTIONAL) final Boolean domingo) {
	//
	// DiasDoctores diasDoctores = new DiasDoctores();
	// diasDoctores.setDoctor(doc);
	// int cont = 0;
	// while (cont < 6) {
	// if (lunes == true) {
	// diasDoctores.setLunes(DiasEnum.LUNES);
	// cont++;
	// } else
	// cont++;
	// if (martes == true) {
	// diasDoctores.setMartes(DiasEnum.MARTES);
	// cont++;
	// } else
	// cont++;
	// if (miercoles == true) {
	// diasDoctores.setMiercoles(DiasEnum.MIERCOLES);
	// cont++;
	// } else
	// cont++;
	// if (jueves == true) {
	// diasDoctores.setJueves(DiasEnum.JUEVES);
	// cont++;
	// } else
	// cont++;
	// if (viernes == true) {
	// diasDoctores.setViernes(DiasEnum.VIERNES);
	// cont++;
	//
	// } else
	// cont++;
	// if (sabado == true) {
	// diasDoctores.setSabado(DiasEnum.SABADO);
	// cont++;
	//
	// } else
	// cont++;
	// if (domingo == true) {
	// diasDoctores.setDomingo(DiasEnum.DOMINGO);
	// cont++;
	// } else
	// cont++;
	// }
	// persist(diasDoctores);
	// return diasDoctores;
	// }

	// @javax.inject.Inject
	// DomainObjectContainer container;
}